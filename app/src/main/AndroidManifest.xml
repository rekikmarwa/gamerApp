<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GamerApp"
        tools:targetApi="31">
        <activity
            android:name=".UISplashScreen"
            android:theme="@style/Base.Theme.UISplashScreen"
            android:exported="true" />

        <activity
            android:name=".UIForgotPassword"
            android:exported="true" />
        <activity
            android:name=".UIResetPassword"
            android:exported="true" />
        <activity
            android:name=".UIOTPValidation"
            android:exported="true" />
        <activity
            android:name=".UISignUp"
            android:exported="true" />
        <activity
            android:name=".UILogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>